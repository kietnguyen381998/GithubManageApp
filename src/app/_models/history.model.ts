export interface ItemHistory {
  query: string,
  owner: string,
  repositoryName: string,
  language: string,
  minSize: number,
  maxSize: number,
  date: string,
  page: number,
  perPage: number,
}
